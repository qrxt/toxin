include ../../pug/mixins.pug

block variables
  -
    const defaultLogoObj = {
      className: "",
      isFull: false,
      link: "/"
    }

mixin logo(logoObj)
  - const { className, isFull, link } = logoObj
  if isFull
    a.logo(class=className, href=link)
      img(
        src="assets/img/logo-text.png"
        alt="Логотип Toxin")
  else
    a.logo(class=className, href=link)
      +svg-gradients()
      +svg("logo-toxin", 45, 45)
