include ../dropdown/dropdown

mixin dropdown-rooms(dropdownRoomsObj)
  -
    const {
      className,
      label,
      rightLabel,
      header,
      title
    } = dropdownRoomsObj

  +dropdown(dropdownRoomsObj)
    block
