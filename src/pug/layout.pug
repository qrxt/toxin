block variables
- let mainTitle = "Toxin"

doctype html
html(lang="ru")
  head
    meta(charset="utf-8")
    title #{ mainTitle }
    meta(
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    )
  body
    .page-wrapper(class=pageWrapperClassName)
      block header
      block content
      block footer
